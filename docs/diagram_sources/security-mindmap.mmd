mindmap
  root((Security Controls))
    trust["Session Trust"]
      rate_limit["Rate limit after 2 uploads"]
      captcha_gate["Math CAPTCHA to unlock trust"]
    hardening["Request Hardening"]
      csrf["CSRF tokens on every write"]
      csp["Nonce-based CSP + strict headers"]
    identity["Identity & Access"]
      magic_links["Passwordless magic links"]
      feedback_guard["Auth required for feedback API"]
    data["Data Protection"]
      temp_cleanup["Ephemeral temp files"]
      validation["Pillow validation prevents abuse"]
      storage["Results stored in SQL + session store"]
